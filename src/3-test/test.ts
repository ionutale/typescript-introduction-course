
// sum 2 numbers
function sum(a, b) {
  return a + b
}

// check boolean
function isEarthFlat(value) {
  if (value === false) {
    return false
  }

  if (value === true) {
    return true
  }

  return true
}

// create a literal type that allows only 'm' or 'f'
function allowsSex(sex) {
  return sex
}



/**** IGNORE THIS PART */
export {
  sum,
  isEarthFlat,
  allowsSex
}
